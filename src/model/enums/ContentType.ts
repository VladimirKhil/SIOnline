/** Defines well-known displayable content type. */
enum ContentType {
    /** Empty content. */
    Void,

    /** Text. */
    Text,

    /** Image. */
    Image,

    /** Video. */
    Video,

    /** HTML page. */
    Html
}

export default ContentType;