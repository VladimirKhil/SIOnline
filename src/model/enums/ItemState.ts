/** Defines table item state. */
enum ItemState {
    /** Normal (default) state. */
    Normal,

    /** Active item. */
    Active,

    /** Item is right. */
    Right,

    /** Item is wrong. */
    Wrong
}

export default ItemState;